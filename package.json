{
  "name": "uppl-mer-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node src/generate.mjs",
    "test": "node test/run-tests.mjs",
    "web": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build"
  }
}
